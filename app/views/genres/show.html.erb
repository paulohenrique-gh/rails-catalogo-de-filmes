<% if flash[:notice] %>
  <div>
    <h3><%= flash[:notice] %></h3>
  </div>
<% end %>

<div>
  <h2><%= @genre.name %></h2>
</div>
<div class="edit">
  <%= link_to 'Editar', edit_genre_path(@genre.id) %>
</div>